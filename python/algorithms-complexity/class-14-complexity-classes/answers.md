# Class 14: complexity classes (translated)

**a) (problems 1 to 5)** One can assert every value of the sum and assume the neighbouring values by induction.

**b)** P1 is polynomial. P2 is polynomial. P3 is NP-complete. P4 is polynomial. P5 is non-deterministic polynomial.

Note: it is technically impossible to count to infinity in useful time, but the value can be approximated with calculus and other formulas.
