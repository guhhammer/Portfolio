# Sorting benchmarks (Go)

One file that implements bubble sort, insertion sort, merge sort, quicksort and heapsort and benchmarks them across array sizes from 10 to tens of millions of elements. Each algorithm has a time and size limit so the slow quadratic sorts stop before they take minutes; the measured timings are printed as a table.

```bash
go run .
```
